# 2026-08-19

## 生成原型图
- 依据 src/ 当前代码（Vue3 + Element Plus + Pinia）为 aier_science_admin 科普馆预约后台生成交互式 HTML 原型图，输出至 `原型图/index.html`。
- 覆盖页面：登录页、科普馆场次配置、科普馆预约查询、活动场次配置、意见反馈列表、意见反馈图表、用户管理（路由未挂载）。
- 含关键弹窗原型：修改密码、科普馆场次添加/批量添加、活动场次添加/批量添加（含 KV 上传、时间段配置、预览）。
- 交互：顶部导航 + 页面内菜单切换，弹窗可开关。配色采用 Element Plus 浅色风格（brand #2563eb）。

## 活动场次配置·二维码放置方案（设计评审）
- 问题：活动场次表格原 14 列、横向溢出、每列过窄；需求为「展示每个活动二维码」。
- 结论方案（已更新到 原型图/index.html 对应屏）：不在行内塞常驻小图列（太小扫不了、更挤）。改为 ——
  1) 表格加「二维码」缩略图列（46px，沿用活动KV的缩略图+点击放大模式），行内一眼可见哪个活动有码；
  2) 点击缩略图或「查看二维码」操作按钮，弹 Dialog 显示 220px 大图（可扫、可下载）；
  3) 为腾空间：移除「活动背景」列（改为详情/编辑弹窗内展示），并将「开始日期/结束日期」合并为「活动日期」一列。
- 对应真实代码落点：`src/views/activity-session-config/index.vue` 的 columns 数组（去掉 theBackground 列、合并日期、加 qrCodeUrl slot 列），操作列现有 `生成二维码` 按钮（handleQrCode 调 activityQrCodeUrlApi）应改为展示已有 qrCodeUrl 的大图弹窗。
- 评审修正：用户指出二维码应与活动KV相邻（同为图像型信息，符合就近/相似原则）。已将「二维码」列移到「活动KV」右侧紧邻（原型已更新）。
