# 2026-06-04

## 活动场次配置模块

- 新增路由 `activity-session-config`（ActivitySessionConfig），icon: Tickets
- AdminLayout.vue 新增 Tickets 图标引入及 iconMap 注册
- `src/api/science.ts` 新增 `ActivitySessionRow`、`ActivitySessionQuery` 接口及 `getActivitySessionList` API
- 新建页面 `src/views/activity-session-config/index.vue`
  - 基于科普馆场次配置，新增：活动标题（el-input）、活动背景（el-textarea）、活动地点（el-input）三个字段
  - 表格列：序号/活动标题/活动背景/活动地点/日期/开始时间/结束时间/总号数/余号/创建时间/操作人/操作
  - 添加/编辑弹窗：新增三个必填字段，校验规则同步添加
  - 批量添加弹窗：同样包含活动标题/背景/地点三个字段，批量生成时每条记录共享这三个值
  - 删除确认文案带上活动标题，体验更友好
  - 模拟数据：3条活动场次示例数据

## 活动场次配置 - 日期拆分

- `ActivitySessionRow` 接口：`date` → `startDate` / `endDate`
- 表格列：日期 → 开始日期 + 结束日期（两列）
- 添加/编辑弹窗：单 date picker → 开始日期 + 结束日期两个独立 el-date-picker
- 校验规则同步更新（startDate、endDate 均为必填）
- 编辑回填、新增提交、删除确认文案均适配新字段

## 科普馆预约查询 - 导出 Excel 按钮

- 页面顶部右侧添加「导出 Excel」按钮（type=success, icon: Download）
- 点击触发 `ElMessageBox.alert` 弹窗提示「该功能正在开发中，敬请期待！」

# 设置白名单
- chrome://flags/#unsafely-treat-insecure-origin-as-secure

## 活动场次配置 - 接入 MSW 模拟 API

- **handlers.ts**：修复 `/api/activity/sessions` handler，从使用错误的 `sessionRows` 改为正确的 `activityRows`，过滤逻辑改为按 `startDate`/`endDate` 匹配
- **activity-session-config/index.vue**：`fetchSessions()` 从硬编码 mock 数据改为调用 `getActivitySessionList(params)` API，与科普馆场次配置保持一致的数据获取方式
