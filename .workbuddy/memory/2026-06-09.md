# 2026-06-09

## 活动场次配置 - 添加/编辑弹窗改造

- `src/api/science.ts`：`ActivitySessionRow` 新增 `coverKey?: string`，`location` 改为可选 `location?: string`
- `src/views/activity-session-config/index.vue`：
  1. **活动 KV**：新增 el-upload 上传组件（action="#"，自定义 http-request），beforeUpload 校验图片类型和 2MB 大小；本地预览用 createObjectURL，coverKey 模拟生成
  2. **活动地点**：移除 required 校验规则和 prop 绑定，改为选填
  3. **时间段**：el-select 固定选项 → 拆成开始时间 + 结束时间两个 el-time-picker（value-format="HH:mm"）
  4. formData 新增 coverKey/coverUrl/startTime/endTime，移除 timeSlot
  5. 编辑回填、提交逻辑、关闭重置均同步适配
  6. 批量弹窗：location 改为非必填，移除对应 batchRules.location
  7. 新增 kv-uploader 相关 scoped style

## 活动场次配置 - 批量添加弹窗新增活动 KV 上传

- 模板：活动背景之后新增 `el-upload` 活动 KV 上传（复用 `handleBeforeUpload` 校验，独立 `handleBatchUploadKV` 处理）
- `batchFormData` 新增 `coverKey: string` / `coverUrl: string`
- `handleBatchUploadKV`：本地预览 + 模拟 key
- `handleBatchDialogClose`：关闭时同步清空 `coverKey`/`coverUrl`
- `handleBatchSubmit`：批量生成场次时携带 `coverKey`

## 活动场次配置 - 批量添加弹窗时间段改造

- 模板：`el-checkbox-group` 多选时间段 → 拆成「开始时间」+「结束时间」两个 `el-time-picker`
- `batchFormData`：移除 `timeSlots: string[]`，新增 `startTime: string`/`endTime: string`
- `batchRules`：移除 `timeSlots` 规则，新增 `startTime`/`endTime` 必填规则
- `watch` 依赖：`timeSlots` → `startTime`/`endTime`
- `generateBatchPreview`：不再遍历时间段数组，直接每天生成一条记录，timeLabel = `${startTime}-${endTime}`
- `handleBatchSubmit`：直接用 `batchFormData.startTime`/`endTime`，移除 `split('-')`
