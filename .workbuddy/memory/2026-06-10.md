# 2026-06-10

## 科普馆场次配置 - 新增讲解服务人数字段

- `src/api/science.ts`：`SessionRow` 新增 `guideCount?: number` 字段
- `src/mocks/handlers.ts`：`sessionRows` 每条数据新增 `guideCount` 示例值
- `src/views/session-config/index.vue`：
  - 表格新增「讲解服务人数」列，未填时显示 `-`
  - 添加/编辑弹窗：总号数之后新增「讲解人数」`el-input-number`（选填，min=0）
  - `formData` 新增 `guideCount: undefined`
  - 编辑回填、新增提交均携带 `guideCount`
  - 批量添加弹窗新增「讲解人数」字段，批量生成场次时携带 `guideCount`
