# 2026-06-03 工作日志

## 任务：解读后台管理项目，生成 README 文档

- 项目：aier-science-admin（爱尔科技后台管理系统）
- 路径：G:\GDProject\all_company_pro\v3_aier_book_admin\aier_science_admin
- 操作：全面探索项目结构（package.json、vite.config.ts、src/ 所有文件），生成 README.md
- 产物：G:\GDProject\all_company_pro\v3_aier_book_admin\aier_science_admin\README.md

**项目技术栈简记**：Vue 3.5 + **Vite 8（Rolldown 引擎）** + TypeScript 5.6（严格） + Vue Router 4 + Pinia 2 + Element Plus 2.9 + Tailwind CSS 3.4 + Axios 1.7  
当前状态：启动模板阶段，登录为 Mock，数据静态硬编码，暂无真实接口

## 任务：科普馆场次配置页面

- 修改 router/index.ts：dashboard → session-config，redirect 及 404 通配符 redirect 同步修改
- 修改 AdminLayout.vue：import DataLine → Calendar，iconMap 同步更新
- 新建 src/views/session-config/index.vue：顶部「添加场次」按钮 + el-table（日期/开始时间/结束时间/总号数/余号 tag/创建时间/操作人/操作列 团队预约·编辑·删除）

## 任务：Vite 6 → Vite 8 升级

- package.json：vite ^6.0.7 → ^8.0.0，@vitejs/plugin-vue ^5.2.1 → ^6.0.0
- vite.config.ts：无需修改（无废弃 API 使用）
- README.md：技术栈、环境要求（Node ≥ 20.19）、目录结构、路由表、布局说明、页面功能描述全部同步更新
- **Vite 8 核心变化**：esbuild + Rollup → Rolldown + Oxc 统一引擎，构建速度提升 10-30x

## 任务：场次配置页添加日期范围查询

- session-config/index.vue 新增查询区域（page-panel 样式卡片）
- 查询条件：开始日期 + 结束日期（el-date-picker, value-format: YYYY-MM-DD, 可清空）
- 按钮：查询（Primary + Search 图标）+ 重置
- 数据过滤逻辑：computed 根据 startDate / endDate 前端过滤 rawData
- Mock 数据扩充至 6 条（跨 06-01 ~ 06-05），覆盖不同余号状态（0/8/15/20/35/50）
- 「添加场次」按钮移至查询区域右侧

## 任务：场次配置页改为手动触发查询

- tableData 从 computed（实时响应）改为 ref，点击「查询」按钮才执行 handleSearch() 过滤
- 选日期不触发任何操作；重置同时恢复全量数据

## 任务：Header 添加面包屑导航

- AdminLayout.vue Header 原标题区域替换为 el-breadcrumb
- 面包屑固定首项「控制面板」（可点击跳转 /），次项为当前页面 meta.title
- 使用 Element Plus el-breadcrumb + el-breadcrumb-item 组件

## 任务：添加场次弹窗功能

- 点击「添加场次」按钮弹出 el-dialog（520px, destroy-on-close）
- 表单字段：日期（el-date-picker）、**时间段（el-select 固定 4 选项）**、总号数（el-input-number）
- 时间段固定选项：09:00-10:30 / 10:30-12:00 / 14:30-16:00 / 16:00-17:00
- 提交时通过 `split('-')` 拆分为 startTime / endTime
- 新数据同时 push 到 rawData 和 tableData，提交按钮带 loading 状态
- 关闭弹窗时自动 resetFields 清空表单

## 任务：删除场次功能

- 删除按钮绑定 `@click="handleDelete(row)"`，操作列 slot 改为 `#default="{ row }"`
- 点击后弹出 ElMessageBox.confirm 二次确认（warning 类型，确认按钮 danger 样式）
- 确认后从 rawData 按 id splice 移除，同步更新 tableData
- 取消则 catch 静默忽略

## 任务：编辑场次功能

- 复用添加弹窗为新增/编辑共用模式，通过 `editId` 区分状态
- 新增 `editId ref<number | null>` + `dialogTitle computed`（有 id 显示「编辑场次」，否则「添加场次」）
- 点击编辑按钮 → `handleEdit(row)`：设置 editId，回填 date / timeSlot（拼接 startTime-endTime）/ totalCount
- 提交时判断 editId：**有值则更新 rawData 中对应记录**，无值则 push 新记录
- 关闭弹窗时同时清空表单和重置 editId 为 null
- 弹窗 title 动态绑定 `:title="dialogTitle"`

## 任务：科普馆预约查询页面（用户管理替换）

- router/index.ts：`users` 路由 → `booking-query`，title「用户管理」→「科普馆预约查询」，icon `User` → `Document`
- AdminLayout.vue：import User 替换为 Document，iconMap 同步更新
- 新建 src/views/booking-query/index.vue：
  - 查询区域同 session-config（开始日期 + 结束日期 + 查询/重置按钮）
  - 表格列：序号 / 姓名 / 手机 / 身份证（等宽字体）/ 成团方式（Tag）/ 团队人数 / 附件（el-link 或 -）/ 日期 / 开始时间 / 结束时间 / 状态（未核销 warning / 已核销 success / 已过期 info）Tag / 创建时间 / 操作（确认参观·取消参观，暂空）
  - Mock 数据 6 条，覆盖 pending/verified/expired 三种状态

## 任务：预约查询页增加筛选项

- 查询区新增 4 个筛选项：
  - **姓名**（el-input，模糊匹配 includes）
  - **手机**（el-input，模糊匹配 includes）
  - **成团方式**（el-select：个人预约 / 团队预约，精确匹配）
  - **状态**（el-select：未核销 / 已核销 / 已过期，精确匹配 status 值）
- handleSearch 改为多条件组合 AND 过滤（所有非空条件取交集）
- handleReset 重置全部 6 个查询条件
- 所有筛选控件均支持 clearable 清空

## 任务：预约查询页增加时间筛选

- 查询区新增 **开始时间** 和 **结束时间** 筛选项（el-time-picker，格式 HH:mm）
- handleSearch 过滤逻辑增加 `row.startTime >= startTime` / `row.endTime <= endTime` 判断
- 重置同步清空时间字段
- 当前筛选项共 **7 个**：开始日期/结束日期/时间段(4固定选项)/姓名/手机/成团方式/状态
- 时间段筛选改为 el-select，与场次配置一致（09:00-10:00 / 10:30-11:30 / 14:30-15:30 / 16:00-17:00）

## 任务：预约查询页手机/身份证完整数据 + 展示脱敏

- rawData 中 phone 改为完整 11 位手机号，idCard 改为完整 18 位身份证号
- 新增 `maskPhone()` 函数：前 3 后 4，中间 ****
- 新增 `maskIdCard()` 函数：前 3 后 4，中间动态 * 填充
- 表格展示时通过模板调用脱敏函数，原始数据保持完整（筛选/接口对接可用原始值）

## 任务：场次配置页增加批量添加功能

- 按钮区新增「批量添加」按钮（success 样式），位于「添加场次」右侧
- 新增批量添加弹窗（560px）：
  - **日期范围**（el-date-picker daterange）
  - **时间段**（el-checkbox-group 多选 4 个固定时段）
  - **总号数**（el-input-number）
  - **预览区域**：watch 监听表单变化实时生成预览列表（日期 × 时间段 = 场次数），可滚动查看
- 提交时遍历预览列表逐条生成 SessionRow push 到 rawData
- 确认按钮显示即将添加的数量 `确认添加 (N)`
