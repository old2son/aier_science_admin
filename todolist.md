# TODO List

## Request / Axios

### 登录失效防重复处理

* [x] 增加 `isRedirecting` 标识
* [x] 防止多个接口同时登录失效时重复执行 `router.replace`
* [x] 防止重复执行 `logout`
* [x] 防止重复清除 xToken

### 请求中断（后续优化）

* [ ] 登录失效后阻止新的请求继续发送
* [ ] 研究 Axios + AbortController
* [ ] 登录失效时取消所有未完成请求
* [ ] 页面切换时取消无效请求

---

## 用户模块

### Token 管理

* [x] Token Key 统一管理
* [x] 检查 logout 是否已负责清理 localStorage
* [x] 避免多处直接操作 localStorage

### 权限管理

* [ ] 确认权限模型
* [ ] 增加权限指令

---

### 多人预约展示

* [x] 优化随行人员 Popover UI
* [x] 适配移动端展示

### 优化
* [x] 预约并发控制（防止超卖）
* [ ] ~~编辑/删除场次限制（已有预约时限制修改关键字段）~~
* [x] 编辑/删除场次警告有预约用户
* [x] JWT 过期统一处理（401 自动跳登录，避免把认证失败当成 500）
* [x] 查询、导出、分页保持一致（导出当前筛选结果，而不是全部）
* [x] 重复提交防护（按钮 loading、接口幂等）
* [ ] 权限模型（不同角色可执行不同操作）
* [x] 操作日志（记录新增、编辑、删除等关键操作）